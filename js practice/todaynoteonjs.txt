ctrl+shift+i - 
alert("hello"); can be strings,number.
var x=10; number boolean string
from typeof function we can check which type of data is

shift+enter for the next line in console
in source we can do code of js

var yourname=prompt("enter your name");
alert("hello " + yourname + ",my name is " + myname );

var print=prompt("enter your message");
alert(print);
 
alert(print.length); //length is the property here

var print=prompt("enter your message");
var length=print.length;
var total=140-length;
alert("you have entered" + length + "Remaining are" + total);

name.slice(0,1);
name.slice(1);
name.slice(-1); // last character

var print=prompt("enter your message");
// var length=print.length;
// var total=140-length;
alert("you have entered" + print.length + "Remaining are" + (140-print.length));
alert(print.slice(0,140));

->var name=prompt("enter your name");
var uppername =name.slice(0,1).toUpperCase();
var lowername =name.slice(1).toLowerCase();
alert(uppername + lowername);

->var name=prompt("enter your name");
alert(name.slice(0,1).toUpperCase() + name.slice(1).toLowerCase());

->console.log (output for us)
-> alert (for the clients/end user)
-> (===)called the identical operator which check the value + datatype too.

var a=10;
var b='10';
if (a===b) {
    alert("values are same");
}else{
    alert("values are different");
}

-> function getMilk() {
    console.log("move up");
    console.log("move down");
    console.log("move");
    console.log("move");
    console.log("get milk");
}

->function getMilk(money) {
    console.log("move up");
    console.log("move down");
    console.log("move");
    console.log("move");
    var numBottles= Math.floor(money/45);
    console.log("get " + numBottles +" bottles of milk" );
    return money%45;
}
console.log("your change amount is " + getMilk(100));